name: NBA Analytics Weekly Retrain

on:
  schedule:
    - cron: "0 4 * * MON"   # Runs every Monday at 4 AM UTC
  workflow_dispatch:

jobs:
  retrain-and-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Fetch live player stats
        run: |
          python scripts/fetch_player_stats.py

      - name: Build training dataset
        run: |
          python scripts/build_features.py

      - name: Retrain model
        run: |
          python scripts/train_model.py

      - name: Run full pipeline
        run: |
          python scripts/setup_all.py

      - name: Upload results
        uses: actions/upload-artifact@v3
        with:
          name: nba-weekly-results
          path: |
            results/
            models/game_predictor.pkl
            models/model_metadata.json
            models/shap_feature_importance.png

      - name: Send Telegram notification
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendDocument \
            -F chat_id=${{ secrets.TELEGRAM_CHANNEL }} \
            -F document=@results/picks.csv \
            -F caption="📅 Weekly retrain complete — fresh NBA picks generated with live stats!\nMetrics and SHAP chart uploaded."