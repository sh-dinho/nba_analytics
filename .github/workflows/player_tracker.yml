name: Weekly Player Tracker

on:
  schedule:
    - cron: "0 3 * * MON"   # Every Monday at 3 AM UTC
  workflow_dispatch:

jobs:
  weekly:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Setup environment
        run: |
          echo "ODDS_API_KEY=${{ secrets.ODDS_API_KEY }}" >> $GITHUB_ENV
          echo "TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}" >> $GITHUB_ENV
          echo "TELEGRAM_CHANNEL=${{ secrets.TELEGRAM_CHANNEL }}" >> $GITHUB_ENV

      - name: Generate weekly snapshots
        run: python scripts/generate_weekly_snapshots.py

      - name: Build player trends
        run: python scripts/player_trends.py

      - name: Build weekly summary
        run: python scripts/weekly_summary.py

      - name: Awards forecasts
        run: python scripts/train_awards.py

      - name: Run tests
        run: pytest --maxfail=1 --disable-warnings -q

      - name: Upload results and logs
        uses: actions/upload-artifact@v4
        with:
          name: weekly-tracker
          path: |
            results/
            setup_log_*.txt

      - name: Notify Telegram
        run: |
          python - <<'PY'
          import os, requests
          token = os.environ["TELEGRAM_BOT_TOKEN"]
          channel = os.environ["TELEGRAM_CHANNEL"]
          run_url = f"{os.environ['GITHUB_SERVER_URL']}/{os.environ['GITHUB_REPOSITORY']}/actions/runs/{os.environ['GITHUB_RUN_ID']}"
          msg = f"📊 Weekly Player Tracker completed.\nResults uploaded.\nSee artifacts: {run_url}"
          requests.post(f"https://api.telegram.org/bot{token}/sendMessage", data={"chat_id": channel, "text": msg})
          PY